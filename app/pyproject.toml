[project]
name = "app"
version = "0.1.0"
description = "HTTP API for CalDAV via caldav"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "caldav>=3.2.1",
    "fastapi[standard]>=0.136.3",
]
